# Core data handling
pandas>=2.0
numpy>=1.24

# Module 1 - expected-output model (RandomForestRegressor)
scikit-learn>=1.3

# Modules 9 & 10 - explainability + both dashboards (app.py and app_simple.py)
shap>=0.44          # optional at runtime: ml_model.py falls back to feature
                     # importance if this import fails, but install it for
                     # the per-alert local explanations in app.py
plotly>=5.18
streamlit>=1.30      # >=1.30 required for st.data_editor used in app_simple.py's
                     # manual-forecast table
